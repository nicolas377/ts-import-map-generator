export function buildImportTree(entrypointLocation: string): void {
  // read entrypoint
  // find imports of entrypoint
  // store in tree (first level)
  // go through each first level branch and create second level branch
  // 
}
